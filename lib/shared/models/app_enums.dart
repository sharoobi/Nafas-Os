enum InterventionType {
  breathing,
  ghostCigarette,
  guardedAudio,
  walk,
  water,
  microCbt,
  driveShield,
  notificationOnly,
}

enum RiskLevel { low, moderate, high, critical }

enum ActivityContext { still, walking, driving, unknown }

enum SupportTone { calm, balanced, challenger }

enum CompanionMode {
  steady,
  drifting,
  pressured,
  combustible,
  cravingWave,
  recovering,
  winning,
}

enum MissionInteractionMode { tapSequence, holdShield, breathCycle }
